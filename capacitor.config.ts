import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.cardiagnostics',
  appName: 'Car Diagnostics AI',
  webDir: 'dist'
};

export default config;
